PORT=8080
NODE_ENV=production

# MongoDB Connection
# Get this from MongoDB Atlas
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/construo-2026?retryWrites=true&w=majority

# 1. Get MongoDB Connection String (MONGO_URI)
# i. Go to MongoDB Atlas: Visit mongodb.com/atlas and sign up/log in (you can use Google).
# ii. Create a Cluster:
# - Click + Create button.
# - Select M0 (Free) (usually on the far right).
# - Choose a provider (AWS) and region closest to you.
# - Click Create Deployment.
# iii. Create a Database User:
# - It will ask you to set up a username and password.
# - Username: admin (or whatever you like).
# - Password: Create a secure password. Write this down or copy it! You will need it later.
# - Click Create Database User.
# iv. Allow Network Access:
# - Scroll down to "Network Access" or "IP Address".
# - Click Add IP Address.
# - Select Allow Access from Anywhere (0.0.0.0/0). This is required for Render/Vercel hosting.
# - Click Confirm.
# v. Get the Connection String:
# - Go back to the Database tab (left sidebar).
# - Click the Connect button on your cluster.
# - Select Drivers (Node.js).
# - Copy the string that looks like this: mongodb+srv://admin:<password>@cluster0.abcde.mongodb.net/?retryWrites=true&w=majority
# - Update Your URI:
# - Paste this into your .env file.
# vi. CRITICAL STEP: Replace <password> with the actual password you created in Step 3. Remove the < and > brackets.
# - Cloudinary Configuration
# - Get these from Cloudinary Dashboard

CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# 2. Get Cloudinary Credentials
# i. Go to Cloudinary: Visit cloudinary.com and sign up for a free account.
# ii. Go to Dashboard: Once logged in, click on "Programmable Media" (icon looks like <>) on the left, then click Dashboard.
# iii. Copy Credentials:
# - At the top of the Dashboard, you will see a section called "Product Environment Credentials".
# - Cloud Name: Copy this value.
# - API Key: Copy this value.
# - API Secret: Click the "eye" icon to reveal it, then copy it.

# JWT Secrets
JWT_SECRET=your_super_secret_jwt_key
JWT_EXPIRES_IN=24h

# RUN COMMAND
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# CORS
CORS_ORIGIN=https://your-app.onrender.com
